# T7 — UX Friction Audit
**Date:** 2026-03-29 00:44
**Container start time:** 8s
**First task latency:** 12s

## Score
- ✓ Pass: 40
- ⚠ Warn: 0
- ✗ Fail: 0
- Total checks: 40

## Findings

- ✓ **help command exits without error**
- ✓ **help lists 'start'**
- ✓ **help lists 'stop'**
- ✓ **help lists 'restart'**
- ✓ **help lists 'status'**
- ✓ **help lists 'logs'**
- ✓ **help lists 'chat'**
- ✓ **help lists 'run'**
- ✓ **help lists 'ask'**
- ✓ **help lists 'task'**
- ✓ **help lists 'task-status'**
- ✓ **help lists 'task-logs'**
- ✓ **help lists 'cancel'**
- ✓ **help lists 'shell'**
- ✓ **help lists 'backup'**
- ✓ **help lists 'restore'**
- ✓ **help lists 'upgrade'**
- ✓ **help lists 'clean'**
- ✓ **help mentions CLAWBOX_DIR env var**
- ✓ **run with no args shows usage**
- ✓ **task with no args shows usage**
- ✓ **cp with no args shows usage**
- ✓ **unknown command shows helpful message**
- ✓ **run with container stopped shows clear warning**
- ✓ **chat with container stopped shows clear warning**
- ✓ **container starts in ≤15s (warm image)**
- ✓ **container reaches healthy state**
- ✓ **first task responds correctly**
- ✓ **first task completes in ≤30s**
- ✓ **status shows container running state**
- ✓ **status shows gateway health**
- ✓ **status mentions connection endpoint**
- ✓ **README documents OPENCLAW_GATEWAY_TOKEN**
- ✓ **README mentions clawbox CLI commands**
- ✓ **README has troubleshooting section**
- ✓ **README explains why token value doesn't matter**
- ✓ **status shows ws:// URL for copy-paste**
- ✓ **version command exists**
- ✓ **short alias 'ask' exists for run**
- ✓ **.clawbox-connect exists for env setup**

## Friction Breakdown

### Startup
- Container start time: 8s
- Time to first agent response (after healthy): 12s

### Help / Discoverability
Clawbox — containerized AI coding agent

Usage: clawbox <command> [args]

Commands:
  start              Start the container
  stop               Stop the container
  restart            Stop then start
  status             Show container + gateway status
  logs               Tail container logs
  chat               Open interactive TUI chat session
  chat --session <name>  Open TUI with a named session (persistent per-project threads)
  run "message"      Send a one-shot message to the agent
  run --context <path> "msg"  Inject file/dir as context before the message
  run --thinking <level> "msg"  Set thinking level (off|minimal|low|medium|high|xhigh)
  run --session <name> "msg"  Use a named session (persistent conversation thread)
  run --quiet "msg"  Suppress status banners; print only the agent's response
  run --json "msg"   Output result as JSON {response, elapsed_ms, timestamp, context_files, session}
  ask "question"     Alias for run — supports the same flags
  task "description" Run a task in the background (output saved to ~/.clawbox-task.log)
  task --context <path> "desc"  Background task with file/dir context
  task --thinking <level> "desc"  Background task with thinking level
  task --session <name> "desc"  Background task in a named session
  task-status        Check background task progress + last task output
  task-logs          Live tail of background task output (tail -f ~/.clawbox-task.log)
  cancel             Cancel the currently running background task
  cp <src> <dest>    Copy file into container (fixes ownership)
  shell              Open a shell inside the container
  logs-tail          Show last 50 lines of today's gateway log
  backup             Backup agent state to tar.gz

### Status Output
```
NAME           IMAGE                  COMMAND                  SERVICE        CREATED          STATUS                    PORTS
clawbox-work   clawbox-clawbox-work   "/sbin/tini -- /home…"   clawbox-work   20 seconds ago   Up 20 seconds (healthy)   127.0.0.1:18790->18789/tcp

Gateway Health
Gateway Health
OK (0ms)
Gateway: ✓ healthy

Connect:  export OPENCLAW_GATEWAY_URL=ws://localhost:18790 OPENCLAW_GATEWAY_TOKEN=clawbox
Run:      clawbox run "your message"
```

## Top UX Recommendations
Based on audit findings, the highest-impact improvements are:
