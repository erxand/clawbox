ANTHROPIC_API_KEY=sk-ant-your-key-here

# Path on your machine where git repos live.
# The container mounts this as /host-repos (read-only) so it can clone from them.
# Default works for the standard openclaw workspace layout.
REPOS_PATH=~/.openclaw/workspace/projects
